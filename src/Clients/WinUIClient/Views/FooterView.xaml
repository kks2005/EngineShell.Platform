<UserControl
    x:Class="WinUIClient.Views.FooterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="OnLoaded">

    <Grid
        Padding="12,8"
        ColumnSpacing="16"
        RowSpacing="6"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Text="{Binding Status, Mode=OneWay}"
            AutomationProperties.AutomationId="Footer.StatusText" />
        <ProgressBar
            Grid.Row="1"
            Height="4"
            Minimum="0"
            Maximum="100"
            Value="{Binding Progress, Mode=OneWay}"
            AutomationProperties.AutomationId="Footer.ProgressBar" />
        <StackPanel
            Grid.Column="1"
            Grid.RowSpan="2"
            Orientation="Horizontal"
            Spacing="8"
            VerticalAlignment="Center">
            <FontIcon
                Glyph="&#xE790;"
                FontFamily="Segoe Fluent Icons"
                FontSize="16" />
            <ComboBox
                x:Name="ThemePicker"
                MinWidth="135"
                SelectionChanged="ThemePicker_SelectionChanged"
                AutomationProperties.Name="App theme"
                AutomationProperties.AutomationId="Footer.ThemeComboBox">
                <ComboBoxItem Content="Light" Tag="Light" />
                <ComboBoxItem Content="Dark" Tag="Dark" />
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
